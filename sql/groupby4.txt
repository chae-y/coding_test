WITH RECURSIVE TIME AS (
    SELECT 0 AS N
    UNION
    SELECT N+1 FROM TIME WHERE N<23
)

SELECT TIME.N, COUNT(HOUR(OUTS.DATETIME))
FROM TIME
LEFT JOIN ANIMAL_OUTS AS OUTS ON TIME.N = HOUR(OUTS.DATETIME)
GROUP BY TIME.N